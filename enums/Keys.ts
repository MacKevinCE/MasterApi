export enum Keys {
    sealedBox = "data",
    where = "where",
    defaults = "defaults",
    update = "update",
    uid = "uid",
    code = "code",
    sql = "sql",
    replacements = "replacements",
    request = "request",
    options = "options",
    records = "records",
    attr = "attr",
    value = "value"
}